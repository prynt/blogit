<%= blog_tag :header do %>
  <%= content_tag(:h1, link_to(post.title, post)) %>
  <%= content_tag(:p, time_tag(post.published_at.to_date, pubdate: true, format: blogit_conf.datetime_format)) if post.published_at? %>
<% end %>
